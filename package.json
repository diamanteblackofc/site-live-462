{
  "name": "terminaldiamanteblac",
  "version": "1.0.0",
  "description": "Terminal-DiamanteBlack — gerado pelo DiamantePink",
  "private": true,
  "scripts": {
    "build": "echo \"Build web já incluído em www/\"",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "apk:debug": "cd android && ./gradlew assembleDebug",
    "apk:release": "cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}